<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= naslov %></title>
    <link rel="stylesheet" href="public.css">
  </head>
  <body>
    <h1><%= kategorija %></h1>
    <div class="film-container">
      <% if(movies.length === 0) { %>
        <p>No movies found.</p>
        <% } else { %> <% movies.forEach(movie => { %>
        <div class="film">
          <a href="/viewmovie/<%= movie._id %>">
            <h2 class="film-title"><%= movie.title %></h2>
          </a>
          <img src="/img/<%= movie.slika %>" alt="picture" /> <br>
    
          <a href="/deletemovie/<%= movie._id %>" class="button">Delete Movie</a>
        </div>
        <% }); %><% } %>
    </div>
   
    <form action="/createmovie" method="POST">
      <label for="title">Title:</label>
      <input type="text" name="title" id="title" /> <br>
      <label for="year">Year</label>
      <input type="number" name="year" id="year" /> <br>
      <label for="imbdRating">IMBD-Rating:</label>
      <input type="text" name="imdbRating" id="imdbRating" /> <br>
      <label for="genre">Genre:</label>
      <input type="text" name="genre" id="genre"> <br>
      <label for="slika">Image:</label>
      <input type="text" name="slika" id="slika" value="default.jpg">
      <button type="submit" name="createMovie">Vnesi nov film</button>
    </form>
  </body>
</html>